name: Telegram Bot

on:
  schedule:
    - cron: '1 */4 * * *'  # Каждые 4 часа в 1 минуту
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: pip install pyrogram TgCrypto
          
      - name: Run Bot
        run: python bot.py
